# no-op: dev slot empty (diag-garage-v1 retired with v2.57 release, 2026-08-02)
# The dev loop skips webhook posts for empty output, so this runs silently once
# per boot. Leave this file as a no-op placeholder; do NOT delete (gh_get would
# log HTTP 404 every 100s poll).
